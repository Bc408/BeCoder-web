---
interface Props {
  label: string;
  title: string;
  description: string;
  imageSrc?: string;
  imageAlt?: string;
  imageWidth?: number;
  imageHeight?: number;
  imageWebpSrcset?: string;
  imageSizes?: string;
  priority?: 'primary' | 'secondary';
}

const { label, title, description, imageSrc, imageAlt = '', imageWidth = 1902, imageHeight = 1386, imageWebpSrcset, imageSizes, priority = 'secondary' } = Astro.props;
---

<figure class:list={['screenshot-frame', priority === 'primary' && 'screenshot-frame-primary']}>
  {imageSrc ? (
    <picture>{imageWebpSrcset && <source srcset={imageWebpSrcset} sizes={imageSizes} type="image/webp" />}<img src={imageSrc} alt={imageAlt} width={imageWidth} height={imageHeight} loading="eager" decoding="async" sizes={imageSizes} /></picture>
  ) : (
    <div class="screenshot-placeholder">
      <p>REAL BECODER SCREENSHOT</p>
      <div>
        <span>{label}</span>
        <h3>{title}</h3>
        <figcaption>{description}</figcaption>
      </div>
      <i aria-hidden="true"></i>
    </div>
  )}
</figure>
